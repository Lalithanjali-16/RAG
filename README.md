## 🔐 Environment Setup

This project uses the OpenAI API. To run it locally, you must create a `.env` file and add your OpenAI API key.

### Steps:

1. Create a file named `.env` in the root directory of the project.
2. Add the following line to the file:

```
OPENAI_API_KEY=your_openai_api_key_here
```

3. Save the file.


### ▶️ Running the Project

After setting up the `.env` file, install dependencies and run the project as usual.

```
pip install -r requirements.txt
python main.py
```

(Adjust the run command based on your entry file.)

---

